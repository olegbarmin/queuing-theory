QueuingModel:
  InputDistribution: # Exponential Distribution
    scale: 100 # average interval between jobs arrival (millis)

  ProcessTimeDistribution: # Gamma Distribution
    shape: 2 # Distribution shape - integer
    scale: 1000 # Distribution rate - average job processing time (millis)

  serversNumber: 2
  queueSize: 5

  simulationDuration: 30000 # Duration of simulation (millis)








